const MessageDisplay = () => {
    return <span style={styleSheet.container}>Showing the Message Display component</span>;
}

const styleSheet = {
    container: {
        margin: '0 auto',
        width: '100%',
        backgroundColor: 'grey',
        height: '100%'
    }
};
export default MessageDisplay;